pyautogui>=0.9.54
Pillow>=10.0.0
# Optional (for confidence-based image matching, e.g. confidence=0.9):
# opencv-python>=4.8.0
# Optional (for global hotkeys: F8/Esc/Ctrl+Enter from any window;
# may require running as Administrator on Windows):
# keyboard>=0.13.5
